# Релизы Imp

Лента автообновления ([appcast.xml](appcast.xml)) и архивы релизов macOS-приложения Imp.
Исходники живут в приватном репозитории `imp-agent`; сюда попадают только собранные
и подписанные сборки. Публикует их `make release` оттуда — руками здесь ничего не меняется.
